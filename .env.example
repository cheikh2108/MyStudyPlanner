# Configuration du port backend (par défaut 4000)
PORT=4000

# Configuration du port frontend (par défaut 5173 via Vite)
# VITE_PORT=5173

# URL de l'API backend pour le frontend
VITE_API_URL=http://localhost:4000
