{
  "name": "mystudyplanner",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "build": "npm run build --workspace frontend && node scripts/copy-frontend-dist.js",
    "start": "npm run start --workspace backend"
  }
}